<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard1.4</TargetFramework>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <Authors>Rami Assi</Authors>
    <Company>ramiassi</Company>
    <Description>Slug: is the part of an URL which identifies a page using human-readable keywords.
This extension for string type adds the Slugify method for generating slugs from any language. This extension method is useful for SEO friendly Urls.
Usage examples:
"Hello World!".Slugify(); // hello-world
"مرحبا بالعالم!".Slugify(); // مرحبا-بالعالم
"Merhaba dünya!".Slugify(); // merhaba-dunya</Description>
    <PackageProjectUrl>https://github.com/ramiassi/Assi.SlugifyExtension.Core</PackageProjectUrl>
    <RepositoryUrl>https://github.com/ramiassi/Assi.SlugifyExtension.Core</RepositoryUrl>
    <RepositoryType>Git</RepositoryType>
    <PackageTags>URL Slug SEO Slugify Web</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Globalization.Extensions" Version="4.3.0" />
  </ItemGroup>

</Project>